@model App.Models.Countries

@{
    ViewData["Title"] = "Delete";
}

@if (TempData["error"] != null)
{
    <div class="alert alert-danger" role="alert">
        @TempData["error"]
    </div>
}
<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Countries</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.COUNTRY_ID)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.COUNTRY_ID)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.COUNTRY_NAME)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.COUNTRY_NAME)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.REGION_ID)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.REGION_ID)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" name="id" value="@Model.COUNTRY_ID" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
