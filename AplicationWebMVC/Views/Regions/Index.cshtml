@model IEnumerable<App.Models.Regions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.REGION_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.REGION_NAME)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.REGION_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.REGION_NAME)
            </td>
           
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.REGION_ID  }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.REGION_ID })
            </td>
        </tr>
}
    </tbody>
</table>
